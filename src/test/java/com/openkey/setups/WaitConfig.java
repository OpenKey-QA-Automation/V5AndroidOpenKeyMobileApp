package com.openkey.setups;

public class WaitConfig extends CapabilitiesManager {

/*
    @BeforeSuite
    //Declare and initialise a fluent wait
    public void setFluentWait() {
        wait = new FluentWait(androidDriver)
            .withTimeout(Duration.ofSeconds(30))
            .pollingEvery(Duration.ofSeconds(30))
            .ignoring(NoSuchElementException.class);
    }

    @BeforeSuite
    //Declare and initialise Implicit wait
    public void setImplicitWait(){
        androidDriver.manage().timeouts().implicitlyWait(10000, TimeUnit.MILLISECONDS);
    }

    Declare and initialise a fluent wait
    public void setFluentWait(AppiumDriver driver) {
       FluentWait fluentWait = new FluentWait<>(driver)
            .withTimeout(Duration.ofSeconds(30))
            .pollingEvery(Duration.ofSeconds(60))
            .ignoring(NoSuchElementException.class);
    }

    public Wait getFluentWait()
    {
        return fluentWait;
    }
*/

}



